services:
  web:
    build: .
    image: "contador-visitas"
    container_name: "visitas"
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    networks:
      - mi-red

networks:
  mi-red:
    driver: bridge
